---
type Props = {
	/** Enables Pinterest Rich Pins metadata. Defaults to true when omitted. */
	richPin?: boolean;
};

const { richPin = true } = Astro.props;
---

<meta name="pinterest-rich-pin" content={richPin ? "true" : "false"} />
